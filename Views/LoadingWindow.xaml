<Window x:Class="PetkusApplication.Views.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Height="250" Width="350">

    <Border Background="Transparent" CornerRadius="10" BorderBrush="Transparent" BorderThickness="2" Padding="20">
        <Grid>
            <!-- Naslov učitavanja -->
            <TextBlock Text="Učitavanje..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Top"
                       Foreground="White" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Margin="0,0,0,20"/>

            <!-- Canvas za raspoređivanje elipsi u sredini prozora -->
            <Canvas x:Name="CirclesContainer" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="200" Margin="0 30 0 0"/>
        </Grid>
    </Border>
</Window>