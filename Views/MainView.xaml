<Window x:Class="PetkusApplication.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:PetkusApplication.Views" 
        Title="MultiTabWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Logout" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Width="100" Height="30" Click="LogoutButton_Click"/>

        <TabControl Grid.Row="1">
            <TabItem Header="FormiranjePonude">
                <ContentControl Content="{Binding FormiranjePonudeView}" />
            </TabItem>
            <TabItem Header="Magacin">
                <ContentControl Content="{Binding MagacinView}" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
