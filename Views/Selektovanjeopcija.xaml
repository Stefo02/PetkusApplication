<Window x:Class="PetkusApplication.Views.Selektovanjeopcija"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Option" Height="200" Width="300">
    <Grid>
        <ListBox x:Name="OptionsListBox" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Button Content="Select" Click="SelectButton_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10"/>
    </Grid>
</Window>
